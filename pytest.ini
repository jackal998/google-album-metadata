[pytest]
testpaths = tests
markers =
    e2e: end-to-end tests that require exiftool to be installed
    local_fixtures: opt-in tests that run against personal Takeout data in
        .local-fixtures/ (gitignored). Skipped automatically when the fixture
        directory is absent. Run with: pytest -m local_fixtures
